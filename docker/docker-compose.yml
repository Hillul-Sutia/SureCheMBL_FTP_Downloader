version: '3.9'

services:
  surechembl:
    build:
      context: ..
      dockerfile: docker/Dockerfile

    container_name: surechembl_pipeline

    volumes:
      - ../data:/app/data

    working_dir: /app

    command: python main.py